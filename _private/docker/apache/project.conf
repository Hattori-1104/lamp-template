# ドキュメントルートの設定
# ${APACHE_DOCUMENT_ROOT} は Apache が起動時に環境変数から展開する
<Directory ${APACHE_DOCUMENT_ROOT}>
    Options -Indexes +FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>

# 開発環境であることを示す（.htaccess の HTTPS リダイレクトを無効化）
SetEnvIf Request_URI "^" APP_ENV=dev